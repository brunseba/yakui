# =============================================================================
# KUBERNETES ADMIN UI - ENVIRONMENT VARIABLES
# =============================================================================
# Copy this file to .env.local and configure for your environment
# 
# CRITICAL: Some variables are REQUIRED for production deployment
# =============================================================================

# =============================================================================
# FRONTEND CONFIGURATION (REQUIRED)
# =============================================================================

# API Base URL - REQUIRED for production
# Points to your backend Kubernetes API server
VITE_API_BASE_URL=http://localhost:3001/api

# API Configuration
VITE_API_TIMEOUT=30000
VITE_API_MAX_RETRIES=3
VITE_API_RETRY_DELAY=1000

# =============================================================================
# BACKEND CONFIGURATION (REQUIRED)  
# =============================================================================

# Kubernetes Cluster Context - OPTIONAL
# If not set, will use current kubectl context (kubectl config current-context)
# Set this to override the default behavior or for production deployments
# CLUSTER_CONTEXT=your-kubernetes-context-name

# API Server Configuration
API_PORT=3001
API_TIMEOUT=30000

# =============================================================================
# PERFORMANCE CONFIGURATION (OPTIONAL)
# =============================================================================

# Resource Limits (prevents performance issues with large clusters)
MAX_RESOURCES_PER_TYPE=100
MAX_NAMESPACES_TO_SCAN=10
MAX_NODES_TO_INCLUDE=50
MAX_CRD_INSTANCES_PER_NS=5
MAX_CRD_SAMPLE_INSTANCES=10

# =============================================================================
# UI CUSTOMIZATION (OPTIONAL)
# =============================================================================

# Dependency Graph Theme Colors (JSON format)
# VITE_DEPENDENCY_THEME_COLORS={"owner":"#ff6b6b","volume":"#4ecdc4","service":"#ffa726"}

# Graph Dimensions
# VITE_DEPENDENCY_NODE_WIDTH=250
# VITE_DEPENDENCY_NODE_HEIGHT=150
# VITE_DEPENDENCY_GRAPH_HEIGHT=600

# =============================================================================
# DEVELOPMENT CONFIGURATION (OPTIONAL)
# =============================================================================

# Enable verbose logging for debugging
ENABLE_VERBOSE_LOGGING=true

# Development mode flags
NODE_ENV=development

# =============================================================================
# COMMON CLUSTER CONTEXTS (EXAMPLES)
# =============================================================================
# Local development:
# CLUSTER_CONTEXT=kind-krateo-quickstart
# CLUSTER_CONTEXT=minikube
# CLUSTER_CONTEXT=docker-desktop

# Cloud providers:
# CLUSTER_CONTEXT=gke_project-id_region_cluster-name
# CLUSTER_CONTEXT=arn:aws:eks:region:account:cluster/cluster-name  
# CLUSTER_CONTEXT=azure-cluster-name

# =============================================================================
# PRODUCTION DEPLOYMENT CHECKLIST
# =============================================================================
# Before deploying to production, ensure:
# 
# 1. ✅ CLUSTER_CONTEXT is set to your production cluster
# 2. ✅ VITE_API_BASE_URL points to your production backend
# 3. ✅ Performance limits are appropriate for your cluster size
# 4. ✅ Remove or set ENABLE_VERBOSE_LOGGING=false for production
# 5. ✅ Test connectivity to your Kubernetes cluster
# 6. ✅ Verify RBAC permissions for the cluster context
#
# Test your configuration with:
# kubectl config use-context $CLUSTER_CONTEXT
# kubectl get nodes
# =============================================================================